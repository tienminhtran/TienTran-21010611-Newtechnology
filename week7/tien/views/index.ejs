<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách Sản phẩm</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Thêm sản phẩm</h1>
    <form action="/" method="post" enctype="multipart/form-data">
        <label for="ma_sp">Mã sản phẩm:</label>
        <input type="number" id="ma_sp" name="ma_sp" required />
        <br />
        <label for="ten_sp">Tên sản phẩm:</label>
        <input type="text" id="ten_sp" name="ten_sp" required />
        <br />
        <label for="so_luong">Số lượng:</label>
        <input type="number" id="so_luong" name="so_luong" required />
        <br />
        <label for="hinh_anh">Hình ảnh:</label>
        <input type="file" id="hinh_anh" name="hinh_anh" accept="image/*" required />
        <br />
        <button type="submit">Thêm</button>
    </form>

    <h1>Danh sách sản phẩm</h1>
    <form action="/delete" method="post">
        <table>
            <tr>
                <th>Chọn</th>
                <th>STT</th>
                <th>Mã sản phẩm</th>
                <th>Tên sản phẩm</th>
                <th>Số lượng</th>
                <th>Hình ảnh</th>
            </tr>
            <% if (items && items.length > 0) { %>
                <% items.forEach((sp, index) => { %>
                    <!-- consolog -->
                    <% console.log(sp); %>
                <tr>
                    <td><input type="checkbox" name="selectedItems" value="<%= sp.ma_sp %>" /></td>
                    <td><%= index + 1 %></td>
                    <td><%= sp.ma_sp %></td>
                    <td><%= sp.ten_sp %></td>
                    <td><%= sp.so_luong %></td>
                    <td>
                        <% console.log("Hình ảnh:", sp.hinh_anh); %>

                        <img src="<%=sp.hinh_anh%>" alt="Hình ảnh sản phẩm" width="50px" height="50px" />
                    </td>
                </tr>
                <% }); %>
            <% } else { %>
                <tr><td colspan="6">Không có sản phẩm nào.</td></tr>
            <% } %>
        </table>
        <button type="submit">Xóa sản phẩm đã chọn</button>
    </form>
</body>
</html>
