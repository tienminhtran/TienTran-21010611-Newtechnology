<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách Sản phẩm</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>

    <h1>Thêm sản phẩm</h1>
    <form action="/" method="post">
        <label for="ma_sp">Mã sản phẩm:</label>
        <input type="number" id="ma_sp" name="ma_sp" required>
        <br>
        <label for="ten_sp">Tên sản phẩm:</label>
        <input type="text" id="ten_sp" name="ten_sp" required>
        <br>
        <label for="so_luong">Số lượng:</label>
        <input type="number" id="so_luong" name="so_luong" required>
        <br>
        <button type="submit">Thêm</button>
    </form>

    <h1>Danh sách sản phẩm</h1>

    <form action="/delete" method="post">
        <table>
            <tr>
                <th>Chọn</th>
                <th>STT</th>
                <th>Mã sản phẩm</th>
                <th>Tên sản phẩm</th>
                <th>Số lượng</th>
            </tr>
            <% if (items && items.length > 0) { %>
                <% for (let i = 0; i < items.length; i++) { %>
                    <tr>
                        <td><input type="checkbox" name="selectedItems" value="<%= items[i].ma_sp %>"></td>
                        <td><%= i + 1 %></td>
                        <td><%= items[i].ma_sp %></td>
                        <td><%= items[i].ten_sp %></td>
                        <td><%= items[i].so_luong %></td>
                    </tr>
                <% } %>
            <% } else { %>
                <tr>
                    <td colspan="5">Không có sản phẩm nào.</td>
                </tr>
            <% } %>
        </table>
        <button type="submit">Xóa sản phẩm đã chọn</button>
    </form>

</body>
</html>
